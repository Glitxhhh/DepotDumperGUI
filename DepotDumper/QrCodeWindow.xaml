<Window x:Class="DepotDumper.GUI.QrCodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Steam Login QR Code"
        Height="650" Width="550"
        MinHeight="500" MinWidth="450"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BgBrush}" Foreground="{DynamicResource TextBrush}" FontFamily="{StaticResource UiFont}"
        ResizeMode="CanResize"
        SizeToContent="Manual">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Scan QR Code with Steam Mobile App"
                   FontSize="16"
                   FontWeight="Bold"
                   Foreground="{DynamicResource TextBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- QR Code Display -->
        <Viewbox Grid.Row="1"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 MaxWidth="400"
                 MaxHeight="400"
                 Margin="10">
            <Border Background="White"
                    CornerRadius="10"
                    Padding="20"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Image x:Name="QrCodeImage"
                       Stretch="None"
                       RenderOptions.BitmapScalingMode="NearestNeighbor"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
            </Border>
        </Viewbox>

        <!-- Instructions -->
        <TextBlock Grid.Row="2"
                   Text="1. Open the Steam Mobile App&#x0a;2. Tap the menu and select 'Sign in with QR Code'&#x0a;3. Scan this code to log in"
                   FontSize="12"
                   Foreground="{DynamicResource MutedBrush}"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   Margin="0,20,0,0"
                   LineHeight="20"/>

        <!-- Close Button -->
        <Button Grid.Row="3"
                x:Name="CloseButton"
                Content="Close"
                Click="CloseButton_Click"
                HorizontalAlignment="Center"
                Padding="40,10"
                Margin="0,20,0,0"
                Style="{StaticResource PrimaryButton}"/>
    </Grid>
</Window>
