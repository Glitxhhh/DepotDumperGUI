# Every push to master is a release; the minor number is bumped automatically (1.3.0 -> 1.4.0).
name: Auto release

on:
  push:
    branches: [ master ]

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/release.yml
    with:
      bump: minor
